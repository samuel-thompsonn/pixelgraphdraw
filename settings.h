#pragma once
#include "linear_utils.h"
const float PI_SUB = 3.14159265358;
const float rotateRate = PI_SUB * 2 / 480.f;
const int windowSize = 300;
const int graphSize = 10;
const float graphScale = 1;
const float displaySize = 80.f;
const int timerSpeed = 20;
const float originX = windowSize / 2;
const float originY = windowSize / 2;